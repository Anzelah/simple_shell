#include "main.h"

/**
 * find_path - handle the ppath
 * @arguent: argument
 *
 * Return: always (0)
 */

/*char *find_path(char *argument)
{
	char *envpath, *envpath_cpy;

	*envpath = getenv("PATH");

	envpath_cpy = strdup(envpath);
	argument_length = _strlen(argument);
	
	tokens = strtok(envath_cpy, ":");
	while (tokens != NULL)
	{
	}
	tokens = strtok(NULL, ":");*/
